function onCreate()

    makeLuaSprite('theDark','farm/farm',-610,-300)
	addLuaSprite('theDark',false)
	close(true)
end

function onBeatHit( ... )--for every beat
	-- body
end

function onStepHit( ... )--for every step
	-- body
end

function onUpdate( ... )
	-- body
end
